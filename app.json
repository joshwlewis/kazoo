{
  "name": "zoo",
  "addons": [
    "heroku-postgresql"
  ],
  "scripts": {
    "test": "bin/rails test && bin/rails test:system --backtrace"
  },
  "environments": {
    "test": {
      "buildpacks": [
        { "url": "https://github.com/heroku/heroku-buildpack-google-chrome#install-xvfb" },
        { "url": "https://github.com/joshwlewis/heroku-buildpack-chromedriver" },
        { "url": "heroku/ruby" }
      ]
    }
  }
}
