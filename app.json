{
  "name": "zoo",
  "addons": [
    "heroku-postgresql"
  ],
  "scripts": {
    "test": "bin/rails test && bin/rails test:system"
  },
  "environments": {
    "test": {
      "env": {
        "GOOGLE_CHROME_CHANNEL": "dev"
      },
      "buildpacks": [
        { "url": "https://github.com/heroku/heroku-buildpack-google-chrome#stable-channel" },
        { "url": "https://github.com/joshwlewis/heroku-buildpack-chromedriver" },
        { "url": "heroku/ruby" }
      ]
    }
  }
}
